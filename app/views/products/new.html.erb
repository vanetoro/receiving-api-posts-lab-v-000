<h1>New Product</h1>
<%= form_for @product do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <%= f.label :price %>
  <%= f.text_field :price %>
  <br />
  <%= f.label :description %>
  <%= f.text_area :description %>
  <br />
  <%= f.label :inventory %>
  <%= f.text_field :inventory %>
  <%= f.submit "submit" %>
<% end %>

<div class="product">
  <p id= "name"></p>
  <p id= 'price'></p>
  <p id= 'description'></p>
  <p id="inventory"></p>
  </div>

<script type="text/javascript" charset="utf-8">
$(function() {
  $('form').submit(function(event) {
    event.preventDefault();
    var values = $(this).serialize();
    var posting = $.post('/products', values)
    posting.done(function(data) {
      var product = data;
      $('#name').text(prduct['name'])
      $('#price').text(prduct['price'])
      $('#description').text(prduct['description'])
      $('#inventory').text(prduct['inventory'])
    })
  })
})

</script>
